import styled from 'styled-components';
import { commonTheme } from '../../styles/common';

// 記錄操作主容器
export const RecordActionsContainer = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 10px;
`;

// 操作按鈕群組
export const RecordActionsButtons = styled.div`
  display: flex;
  gap: 10px;
  align-items: center;
`;