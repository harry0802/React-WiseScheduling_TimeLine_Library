import React from "react";
import MachineBoard from "../components/WiseScheduling/components/machine/board/MachineBoard";

/**
 * @component MachineStatusBoard
 * @description 機台狀態操作面板頁面 - 原 WiseSchedulingIndex 功能
 * @returns {JSX.Element} 機台狀態看板頁面
 *
 * @notes
 * - 整合機台狀態管理和監控功能
 * - 提供即時機台狀態顯示
 * - 支援機台狀態切換操作
 */
const MachineStatusBoard = () => {
  return <MachineBoard />;
};

export default MachineStatusBoard;